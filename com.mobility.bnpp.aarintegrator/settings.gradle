include "AARDecomposerGroovy"

rootProject.children.each{
	switch(it.name){
		case 'AARDecomposerGroovy':
			it.name = 'AARDecomposerGroovy'
			break
		default:
			println 'Problem Occured'
			break
	}
}